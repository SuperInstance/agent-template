# Migrations

State migration scripts for evolving agent data formats over time.

## Usage

Place numbered migration scripts here (e.g., `001_initial.py`, `002_add_thresholds.py`).

Migrations run in order when the agent starts up, bringing state forward to the current schema.
